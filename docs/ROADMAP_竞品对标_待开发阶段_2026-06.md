# 竞品对标 · 待开发阶段路线图（可勾选）

> 建档 2026-06-25。基于 `git log` + 代码实况 + 三份竞品/进度文档汇总：
> - `docs/陪伴产品_分阶段开发与进度_DEVLOG.md`（DEVLOG，§7.2 下一批阶段 + 各阶段「下一步」）
> - `docs/AI跨境电商客服平台_升级开发文档_v2_落地优化版.md`（v2，§9.4 剩余工作）
> - `docs/竞品对比与市场定位_2026.md` / `docs/竞品分析_云译YunYi_与实时翻译赛道_2026.md`
>
> **本文是「待办权威清单」**，与 DEVLOG（已完成回写）互补。重入时：先看本文勾选状态，再到 DEVLOG 对应阶段回写。
> 注：旧根目录 `TODO_NEXT.md` 是 2026-03 历史日志（含虚构模型占位，见 CLAUDE.md 警示），已不维护，**以本文为准**。

图例：`⬜` 未开始 · `🔄` 进行中 · `✅` 完成 · `⏸` 暂挂（等外部资源）
自治性：`[自治]` 代码+单测可独立完成 · `[需外部]` 需真号/真机/GPU/企业 token/Vision key

---

## Tier 1 — 当前前沿收口（桌面多平台内嵌 + 工作台地基）

| # | 状态 | 阶段 | 目标 | 自治性 | 对标竞品差距 |
|---|---|---|---|---|---|
| 1 | ✅ | **D3 一号一指纹注入** | `desktop/inject/fingerprint.js` + 后端按 account_id 确定性派生指纹，覆盖 navigator/Intl/WebGL/Canvas，多号内嵌互不关联防关联封（DEVLOG §85，commit `cc35a5a`；含注入核心下沉 shared/ + 浏览器扩展通道） | [自治] | onechat/ChatX 多号风控 |
| 2 | ⬜ | **D4 双向收件箱桥 + 受控 autopilot** | 内嵌账号变收件箱一等公民；autopilot 接 send-gate / kill-switch；浏览器扩展通道(extension/)回流打通 | [自治] | engagelab journey 雏形 |
| 3 | ⏸ | **四平台选择器现场校准** | 真号 F12 校准 IG/Messenger/X/Zalo 选择器，经覆写层打开 `canIngest`（当前默认关，宁缺毋错） | [需外部]（真号） | 云译/海王平台广度 |
| 4 | ⬜ | **注入失配持续告警升级** | D1c 现为即时 toast；加「失配持续 N 分钟 → 进 alerts 通知流」+ 历史趋势曲线 | [自治] | 运维韧性反超 |
| 5 | ⬜ | **A1 读路径迁移** | 统一收件箱从 `src/inbox/store.py` 读，替代实时聚合 → 解锁跨平台历史 / SLA（`read_from_store` 灰度转默认） | [自治] | — |
| 6 | ⬜ | **稳定平台 message id 收尾** | `extract_platform_msg_id` 白名单覆盖收口，彻底替代 hash 去重 | [自治] | — |

**验收**：D3/D4 单测全绿 + 全量回归不降；选择器校准后 `canIngest=true` 真号回流不脏；A1 切默认读 store 后 `/chats`+`/thread` 行为不变。

---

## Tier 2 — 电商事实深度（对标 Gorgias，补 v2 最大内核缺口）

| # | 状态 | 阶段 | 目标 | 自治性 | 来源 |
|---|---|---|---|---|---|
| 7 | ⬜ | **D1 连接器补齐** | WooCommerce 订单 + 库存/SKU 查询 + 退款政策判定（现仅 Shopify 订单 ✅ + AfterShip 物流 ✅，D1 完成度 ~65%） | [自治]（mock 可测，真查需店铺 key） | v2 §9.1 |
| 8 | ⬜ | **FAQ 自动解决率度量 ≥50%** | 接真实 KB sqlite 跑 FAQ 评测，进 CI 门禁（意图策展集已 100%，**FAQ 解决率尚未度量**——对外唯一可信卖点） | [自治]（CI 缺库优雅跳过） | v2 §9.4-7 |

**验收**：D1 连接器查询走「只读不可编」+ 事实校验注入；FAQ 解决率有数字、进 `tests` gate（≥50% 不达即 fail）。

---

## Tier 3 — 商业化与翻译质量

| # | 状态 | 阶段 | 目标 | 自治性 | 来源 |
|---|---|---|---|---|---|
| 9 | ⬜ | **真多租户 SaaS** | 全表 `tenant_id` + 查询隔离（License/Billing/席位强制已闭环，缺真隔离）——属大版本+高回归风险 | [自治]（大改） | DEVLOG §J / v2 §9.4-6 |
| 10 | ⬜ | **L 文档翻译 + AI 润色** | docx/pdf 分段保格式整篇翻译 + AI 润色/改写按钮（全仓真空白） | [自治] | DEVLOG §15 / 对标 DeepL |
| 11 | ⏸ | **媒体 AI 真陪伴** | faceswap 真实接入 + 实时语音陪伴（ASR 入站→TTS 出站闭环）；客户端已就绪 | [需外部]（GPU/语音主机） | DEVLOG §7.2 I / 对标 onechat |

---

## 横切 / 运维增强

| # | 状态 | 项 | 目标 | 自治性 |
|---|---|---|---|---|
| 12 | ⬜ | WA/Zalo 模板消息回退 | 24h 客服窗口外自由文本被官方拒 → 接模板消息 + window_expired 转人工镜像 | [需外部]（平台审批模板） |
| 13 | ⬜ | 官方通道送达率看板 | 把 `error_kind` 汇成三端送达率看板 | [自治] |
| 14 | ⬜ | F+2 会话引擎偏好可视 | 抬头显示当前 `pref_engine` 徽标 + 一键清除 + 离线兜底提示 | [自治] |
| 15 | ⬜ | 营销 journey 编排 | 合规再激活之上的轻量 journey 编排（对标 engagelab，与防封号配额并存） | [自治] |

---

## 刻意不做（与「不烧号陪伴」北极星冲突）

- ❌ 大规模群发 / 批量加粉营销（封号风险）
- ❌ 朋友圈 / 状态刷屏营销
- ✅ 可补：客户分组标签等「关系深度」侧 CRM（纯获客群发不做）

---

## 竞品 → 待办映射速查

| 竞品 | 它的杀手锏 | 我方仍存 GAP → 对应待办 |
|---|---|---|
| 云译 YunYi / 海王 | 多平台内嵌 + SCRM | 四平台校准(#3) + 客户分组标签 UI |
| DeepL | 文档翻译 + 润色 | L 文档翻译(#10) |
| onechat | 换脸/语音克隆虚拟人 | 媒体 AI 真陪伴(#11) |
| engagelab | 官方 API 全渠道 + journey | 营销 journey 编排(#15)（官方三端+IG/Zalo 已补齐 ✅） |
| 拓译 tranlico | 多线路对照 | 基本追平（F+ 已落地），仅 #14 可视化增强 |
| Gorgias | 订单上下文工单 | D1 连接器(#7) + FAQ 解决率(#8) |

---

*更新规则：每完成一项把 `⬜→✅` 并在对应 DEVLOG 阶段回写测试结果；新竞品/新缺口在此追加行。以 `git log` + `grep` 验证代码实况优先于本文。*
